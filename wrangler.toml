name = "grist-csv-downloader"
main = "src/index.js"
compatibility_date = "2024-01-01"

# R2 bucket binding for storing processed data
[[r2_buckets]]
binding = "IMMERSION_BUCKET"
bucket_name = "immersion-credits"

# Scheduled trigger - runs every hour
[triggers]
crons = ["0 * * * *"]
